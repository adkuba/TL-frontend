runtime: nodejs
instance_class: F1

handlers:
handlers:
    # Serve all static files with urls ending with a file extension
  - url: /(.*\..+)$ 
    upload: (.*\..+)$
    secure: always
    
    #https
  - url: /.*
    script: auto
    secure: always
    redirect_http_response_code: 301
